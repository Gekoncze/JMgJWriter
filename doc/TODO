### NOW ###
    ...

### SOON ###
    add JAnnotype writer + test

### LATER ###
    consider adding more validations to token writers
        for example words with alphanumeric characters only
        or only a specific list of special characters
        we could also consider making the validations optional (configurable)