### NOW ###
    ...

### SOON ###
    add JEnum writer + test
    add JRecord writer + test
    add JAnnotype writer + test
    add JWriter + test

### LATER ###
    add support for inner classes
    add support for static initializers
    add support for multi-line token, expression and implementation writers
