### NOW ###
    ...

### SOON ###
    add JClassWriter + test
    add JInterface writer + test
    add JEnum writer + test
    add JWriter + test

### LATER ###
    add support for multi-line token and expression writers
    add support for multi-line annotations
    add support for records
